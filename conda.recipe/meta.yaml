package:
  name: aglcheck
  version: "0.2.0b"

source:
  #git-rev: v0.2.0b
  git-url: https://github.com/gjlbeckers-uu/aglcheck.git

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - pyyaml
    - numpy
    - matplotlib

test:
  requires:
    - yaml
  imports:
    - aglcheck

about:
  license: BSD
  license_file: LICENSE