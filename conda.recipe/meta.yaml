package:
  name: aglcheck
  version: "0.1.0"

source:
  git-url: https://github.com/gjlbeckers-uu/aglcheck.git
  git-rev: v0.1.0

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - pyyaml
    - numpy
    - matplotlib

test:
  requires:
    - yaml
  imports:
    - aglcheck

about:
  license: BSD
  license_file: LICENSE